<% flash.each do |key, value| %>
  <div class="flash <%= flash_background_color(key) %> animate-slide-out-top flex items-center p-4 mb-4  "><i class="fa-solid fa-circle-info"></i> <%= value %></div>
<% end %>